# Deferred Work

- Add duplicate-bill controls for tenant/month idempotency if this demo becomes an operational billing tool.
- Add export/import or backup support for `localStorage` data before relying on the app for real records.
- Add charge effective dates or active/inactive lifecycle rules for recurring charges if historical recomputation becomes required.
- Add schema versioning/migrations if stored records need to survive future app upgrades.
